# Results Policy

This directory stores generated outputs from simulation runs.

- By default, newly generated run outputs are ignored by Git.
- `20251218_172404/` is tracked as a representative reproducibility sample.
- Large binary artifacts such as `results.pkl` are intentionally excluded.
